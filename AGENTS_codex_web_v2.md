# AGENTS.md

## 项目名称
霸王大陆2 配置查询器

---

## 一、项目定位
这是一个**只读型内部配置查询工具**，不是编辑器，不做在线改表，不做写回服务端。

目标是基于 `data/current/` 中的游戏配置表，逐步构建一个**可查询、可追溯、可解释、可关联**的内部运营工具，服务于以下四个业务域：

1. 物品查询
2. 强化查询
3. 任务查询
4. 礼包配置查询

最终目标不是“展示单张表”，而是实现：

- 输入一个物品 / 任务 / 礼包 / 强化配置 ID 或名称
- 系统自动聚合并展示与其相关的所有表、字段、枚举、奖励、关系链路与候选解释
- 所有展示结果都能追溯到原始表、原始字段、原始值

---

## 二、项目背景与事实边界
1. 当前仅有配置表，没有完整客户端和服务端源码。
2. 本项目只负责读取、分析、索引、聚合、展示，不负责修改原始配置。
3. 对无法确认的字段、枚举、关系、业务语义，**禁止强行下结论**，必须标记为“待确认”。
4. 首轮目标不是直接做完整网页，而是先建立**稳定的数据底座 + 索引层 + 可追溯分析产物**。
5. 本项目应优先适配 **Codex Web + GitHub + Cloud Environments** 的工作模式，而不是默认依赖本地 Windows 环境。

---

## 三、执行模式要求（非常重要）
### 默认模式
优先使用：
- Codex Web
- GitHub 仓库
- Cloud Environments

### 原因
1. 本项目涉及约 90 张表的批量扫描、结构识别、索引生成与多轮迭代。
2. 这类任务更适合在**可复现的云端容器环境**中执行，而不是依赖个人电脑的临时环境状态。
3. 本项目的环境、依赖、输出物、约束必须写入仓库，让 Codex 能在云端稳定执行。

### 本地模式定位
Codex App / IDE / CLI 仅作为辅助模式：
- 本地查看结果
- 本地跑少量脚本
- 本地验证网页效果
- 本地调试少数失败任务

**禁止把“必须先在我的 Windows 本机全部跑通”作为项目第一前提。**

---

## 四、强制实现约束（必须遵守）
1. 禁止使用 PowerShell 逐行解析 CSV / TSV / 文本表。
2. 禁止通过 `Get-Content` / `Select-Object` / `Add-Type` 等方式构建表扫描主逻辑。
3. 表解析必须使用 Python 实现。
4. 首选 `pandas`；若格式或编码不兼容，再退回 Python 标准库 `csv`。
5. 首轮只做**数据摄取、结构扫描、索引生成、候选关系输出**，不做完整前端页面开发。
6. 任何时候都不要把“候选字段解释”写死成最终业务语义。
7. 原始表文件只读，禁止改写 `data/current/` 中的文件。
8. 所有脚本必须可重复运行，允许删除旧产物后再次全量生成。
9. 所有输出必须可追溯到原始文件名、表名、字段名、行号或主键值。
10. 所有不确定结论必须显式标为“待确认”。

---

## 五、推荐技术栈（按阶段划分）

### 第一阶段：数据底座
必须具备：
- Python 3.11+
- pandas
- sqlite3（标准库即可）
- pathlib
- json
- re
- csv
- typing
- logging

建议加入：
- charset-normalizer（编码识别）
- PyYAML（规则文件）
- python-dateutil（若表中存在复杂日期字段）
- tabulate（命令行摘要输出，可选）

### 第二阶段：查询 API
建议使用：
- FastAPI
- uvicorn
- pydantic
- SQLAlchemy（可选；如果直接用 sqlite 查询也可）

### 第三阶段：网页查询端
建议使用：
- React + Vite
- 或 Next.js

但**第三阶段之前禁止抢跑 UI**。如果前两阶段未完成，不要开始构建完整前端。

---

## 六、仓库目录约定

### 输入
- `data/current/`
  当前正在使用的一套配置表，只读

### 中间产物
- `data/cache/`
  放标准化缓存、清洗结果、schema 信息、失败清单

### 索引产物
- `data/indexes/`
  放表索引、字段索引、枚举分布、候选关系、候选业务域映射

### 本地数据库
- `data/db/config_tables.sqlite`
  用于统一查询

### 历史快照
- `data/snapshots/`
  仅用于手工备份与回滚，不参与默认查询

### 规则目录
- `rules/`
  放业务域规则、候选映射规则、人工确认规则

### 脚本目录
- `scripts/`
  放扫描、导入、索引、验证、导出脚本

### 文档目录
- `docs/`
  放扫描报告、候选关系报告、字段字典草案、阶段总结

---

## 七、第一阶段工作目标
第一阶段只完成以下六件事：

1. 扫描 `data/current/` 下全部表文件
2. 自动识别文件编码、分隔符、字段结构与基础类型特征
3. 把全部表导入 SQLite
4. 生成 schema / 字段 / 枚举 / 候选关系索引
5. 输出候选业务域识别报告与待确认字段报告
6. 形成一条“按物品聚合查询”的最小闭环能力（命令行或简单 API 即可，不要求网页）

---

## 八、第一阶段必须输出的产物

### 数据文件
必须输出：
- `data/cache/tables_manifest.json`
- `data/cache/raw_schema.json`
- `data/cache/parse_failures.json`
- `data/db/config_tables.sqlite`
- `data/indexes/table_registry.json`
- `data/indexes/field_registry.json`
- `data/indexes/enum_profiles.json`
- `data/indexes/relation_candidates.json`
- `data/indexes/domain_candidates.json`

### 文档
必须输出：
- `docs/scan_summary.md`
- `docs/candidate_tables.md`
- `docs/candidate_relations.md`
- `docs/field_dictionary_draft.md`
- `docs/unknown_fields_todo.md`
- `docs/item_query_minimum_path.md`

---

## 九、业务域识别要求

### 物品
识别最像以下角色的表：
- 物品主表
- 物品分类表
- 物品属性补充表
- 物品关联奖励表
- 物品来源表
- 物品消耗 / 使用规则表

### 强化
识别最像以下角色的表：
- 强化等级表
- 强化成功率表
- 强化失败规则表
- 强化属性成长表
- 强化消耗表
- 强化材料映射表

### 任务
识别最像以下角色的表：
- 任务主表
- 任务链关联表
- 任务奖励表
- 任务提交条件表
- 任务目标表
- 任务触发 / 完成规则表

### 礼包
识别最像以下角色的表：
- 礼包主表
- 礼包内容物表
- 奖励组映射表
- 发放条件表
- 渠道或活动绑定表

---

## 十、规则机制
规则允许后补，不要求一开始就知道精确字段名。

规则文件位置：
- `rules/item_rules.yaml`
- `rules/enhancement_rules.yaml`
- `rules/quest_rules.yaml`
- `rules/giftpack_rules.yaml`
- `rules/manual_confirmations.yaml`

规则当前仅作为占位，不要求首轮完成业务含义绑定。

---

## 十一、跨表关系识别策略
关系只允许按“候选”输出，不能直接当真相。

优先识别以下关系：
1. 同名字段关系（如 `ItemID`、`RewardID`、`QuestID`）
2. 主键 / 外键形态关系
3. 枚举映射关系
4. 奖励组 / 掉落组 / 礼包组映射关系
5. 任务链前后置关系
6. 强化等级递进关系
7. 文本重复 / 名称近似带来的弱关系

每条候选关系都必须包含：
- 来源表
- 来源字段
- 目标表
- 目标字段
- 证据类型
- 置信度
- 是否待确认

---

## 十二、按“搜一个物品”构建最小闭环
首轮无需网页，但必须为后续网页准备好最小查询闭环。

输入：
- 物品 ID / TID / 名称关键词

输出至少包括：
- 命中的物品主表记录
- 同 ID / 同名称在其他表中的候选记录
- 可能的奖励组 / 礼包 / 任务 / 强化消耗关联
- 涉及字段说明（已确认 / 待确认）
- 关系证据来源

这部分可以先做成：
- Python 命令行查询脚本
- 或 FastAPI 单个接口

---

## 十三、Codex 执行要求

### 任务拆分原则
禁止一次性下达“把整个系统做完”。
必须按以下顺序拆分：

1. 建仓库目录与依赖文件
2. 写表扫描脚本
3. 写 SQLite 导入脚本
4. 写索引生成脚本
5. 写候选关系分析脚本
6. 写最小查询闭环
7. 最后才考虑 API / UI

### 每轮任务都要满足
- 有明确输入
- 有明确输出文件
- 有明确验收标准
- 改动尽量集中在少量文件
- 失败时必须记录失败原因，不可静默略过

### Codex 禁止行为
1. 禁止跳过扫描阶段直接写业务查询页面
2. 禁止为了让结果“看起来完整”而硬编字段意义
3. 禁止在没有失败清单时宣称“全部表已成功处理” 
4. 禁止把临时脚本散落在仓库根目录
5. 禁止引入与当前阶段无关的大型前端框架配置

---

## 十四、建议补齐的仓库文件
为提高 Codex Web / Cloud 成功率，仓库中应尽早具备：

- `README.md`
- `AGENTS.md`
- `requirements.txt`
- `.gitignore`
- `scripts/scan_tables.py`
- `scripts/import_sqlite.py`
- `scripts/build_indexes.py`
- `scripts/query_item.py`
- `rules/*.yaml`

如使用 Cloud Environments，允许通过 setup 自动安装依赖，但项目依赖清单仍必须显式写入仓库。

---

## 十五、首轮成功标准
如果以下条件全部满足，则第一阶段算成功：

1. `data/current/` 所有表都成功导入，或有明确失败记录
2. SQLite 成功生成
3. schema、字段索引、枚举索引、候选关系索引成功生成
4. 四个业务域至少都有一版候选表识别结果
5. 至少形成一版待确认字段报告
6. 已具备“按物品查询”的最小闭环能力
7. 全部结果可重复生成，不依赖手工临时操作

---

## 十六、第二阶段目标（不要提前实现）
第一阶段完成后，再进入第二阶段：

1. 基于 SQLite 建查询 API
2. 提供按物品 / 任务 / 礼包 / 强化的聚合查询接口
3. 支持模糊搜索、ID 搜索、候选关系展开
4. 支持“已确认 / 待确认”区分展示
5. 最后再做网页端查询界面

---


## 十六点五、当前短期执行重点（第二阶段小步迭代）

在不改变长期路线（先脚本、后 API、再 UI）的前提下，当前迭代重点为：

1. 优先提升单物品 TID 查询可用性（命中率、可解释性、追溯性）
2. 优先对 `query_item`、`build_indexes`、`domain_hints` 做小补丁
3. 持续保持“候选 / 待确认”边界，不将候选语义直接固化为最终结论
4. 继续覆盖任务、礼包、强化等关键业务路径
5. 暂不进入正式前端开发

---

## 十七、给 Codex 的执行口径
当需求不明确时，遵循以下优先级：

1. 先保证数据底座可运行
2. 先脚本、后 API、再 UI
3. 先可追溯，再追求解释完整
4. 先输出候选，再等待人工确认
5. 先保证全量扫描稳定，再做高层业务聚合

如果某个字段语义无法确认：
- 不要猜测为最终结论
- 允许给出候选解释
- 必须写入待确认报告

